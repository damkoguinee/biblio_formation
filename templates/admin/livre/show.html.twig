{% extends 'admin_base.html.twig' %}

{% block title %}Livre{% endblock %}

{% block body %}
    <h1>Livre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ livre.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ livre.titre }}</td>
            </tr>
            <tr>
                <th>Resume</th>
                <td>{{ livre.resume }}</td>
            </tr>
            <tr>
            <th>Auteur</th>
            <td><a href="{{path('app_admin_auteur_show', {'id': livre.auteur.id}) }}">{{ livre.auteur.identite }}</a></td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_livre_index') }}">back to list</a>

    <a href="{{ path('app_admin_livre_edit', {'id': livre.id}) }}"><i class="fa fa-edit"></i></a>

    {{ include('_common/_delete_form.html.twig') }}
{% endblock %}
